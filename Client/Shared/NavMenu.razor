@inject IJSRuntime JSRuntime


@* 
@* <div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Portfolio</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
             <div class="my-sidebar-fonts">
                                        <div>
    <button class="my-sidebar-buttons-about" @onclick="ScrollToAbout"></button> 
    About
                                            </div>

                                       <div>
    <button class="my-sidebar-buttons-knowledge" @onclick="ScrollToKnowledge"></button>Knowledge
                                            </div>

                                       <div>
    <button class="my-sidebar-buttons-experience" @onclick="ScrollToExperience"></button>Experience
                                            </div>


                                       <div>
    <button class="my-sidebar-buttons-projects" @onclick="ScrollToProjects"></button>Projects
                                            </div>
 </div>
        </div>
    </nav>
</div> *@



@*---------------------------------------------------------------------------JavaScript--------------------------------------------------------------------- *@
@* @code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}

@code {
    private async Task ScrollToAbout()
    {
        await JSRuntime.InvokeVoidAsync("smoothScrollToElement", "about");
    }
}
@code {
    private async Task ScrollToKnowledge()
    {
        await JSRuntime.InvokeVoidAsync("smoothScrollToElement", "knowledge");
    }
}
@code {
    private async Task ScrollToExperience()
    {
        await JSRuntime.InvokeVoidAsync("smoothScrollToElement", "experience");
    }
}
@code {
    private async Task ScrollToProjects()
    {
        await JSRuntime.InvokeVoidAsync("smoothScrollToElement", "projects");
    }
} *@